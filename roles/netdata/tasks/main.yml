- name: Install netdata packages
  package:
    name: 
      - netdata
    state: present
 
 - name: Start service netdata if not started
  service:
    name: netdata
    state: started
 
  tags:
    - netdata
